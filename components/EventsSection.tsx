export default function EventsSection() {
    return (
        <section id = "events" className = "min-h-screen flex-col items-center justify-center border-t border-white/10 px-6">
            <h2 className = "text-4xl font-semibold mb-6">
                Upcoming Events
            </h2>

            <div className = "text-white/70 text-center">
                <p> Afterlight - 3 April</p>
                <p> Evol, Cape Town</p>
            </div>
        </section>
    );
}